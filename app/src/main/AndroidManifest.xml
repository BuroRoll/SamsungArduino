<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.buror.samsungproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Chat.ChatActivity"
            android:label="Chat">
        </activity>
        <activity
            android:name=".Books_lists.Book0Activity"
            android:label="Начало работы с Arduino">
        </activity>>
        <activity
            android:name=".Items_list.Item0Activity"
            android:label="Начало работы с Arduino">
        </activity>>
        <activity android:name=".IDE.IDEActivity"/>
        <activity android:name=".IDE.Activity_add"/>
        <activity android:name=".IDE.Info"/>
        <activity android:name=".Projects.Internet"/>
    </application>

</manifest>